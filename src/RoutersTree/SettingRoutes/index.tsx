import styled from "styled-components";
import SettingLayout from "../../common/layout/SettingLayout";

export interface SettingRoutesProps {

};

function SettingRoutes(props: SettingRoutesProps) {
  
  return (
    <StyledSettingRoutes>
      <SettingLayout>
      
      </SettingLayout>
    </StyledSettingRoutes>
  )
};

export default SettingRoutes;

const StyledSettingRoutes = styled.div`

`;
